"use client";
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#fb8c00",
      contrastText: "#1f030a",
    },
    secondary: {
      main: "#E74649",
    },
  },
});

export default theme;
